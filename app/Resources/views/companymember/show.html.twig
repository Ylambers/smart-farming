{% extends 'base.html.twig' %}

{% block body %}
    <h1>Companymember</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ companyMember.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('user_companymember_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('user_companymember_edit', { 'id': companyMember.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
