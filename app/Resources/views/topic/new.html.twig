{% extends '::base.html.twig' %}

{% block content %}
<div class="container top-container">
    <h1>Maak een nieuwe vraag</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default" value="Maak nieuwe vraag aan" />
    {{ form_end(form) }}

    <a href="{{ path('topic_index') }}" class="btn btn-default">Terug naar de lijst</a>
</div>
{% endblock %}
