{% extends '::base.html.twig' %}

{% block content %}
<div class="container top-container">
    <h1>Companymemberfunction creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('user_companymemberfunction_index') }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
