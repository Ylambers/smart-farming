{% extends '::base.html.twig' %}

{% block content %}
<div class="container top-container">
    <h1>Bewerk uw bedrijf</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="btn btn-default" value="Bewerk uw bedrijf" />
    {{ form_end(edit_form) }}

        <a href="{{ path('company_index') }}" class="btn btn-default">Terug naar het overzicht</a>

</div>
{% endblock %}
