{% extends '::base.html.twig' %}

{% block content %}
<div class="container top-container">
    <h1>Bekijk uw bedrijf</h1>

    <table class="table  table-striped table-hover">
        <tbody>
            <tr>
                <th>Titel</th>
                <td>{{ company.title }}</td>
            </tr>
            <tr>
                <th>Beschrijving</th>
                <td>{{ company.description }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ company.websiteUrl }}</td>
            </tr>
            <tr>
                <th>Kvk</th>
                <td>{{ company.kvk }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-default" value="Verwijder uw bedrijf">
            {{ form_end(delete_form) }}
    </ul>
</div>
{% endblock %}
