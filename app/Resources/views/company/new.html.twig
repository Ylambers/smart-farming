{% extends '::base.html.twig' %}

{% block content %}
<div class="container top-container">
    <h1>Maak een bedrijf aan</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('company_index') }}">Terug naar overzicht</a>
        </li>
    </ul>
</div>
{% endblock %}
